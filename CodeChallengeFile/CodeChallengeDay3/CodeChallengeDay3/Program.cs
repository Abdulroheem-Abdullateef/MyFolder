using System;
using System.Collections.Generic;

namespace CodeChallengeDay3
{
    class Program
    {
        static void Main(string[] args)
        {


            //Console.Write("Enter the number of the rows: ");
            //int rows = int.Parse(Console.ReadLine());
            //Console.Write("Enter the number of the columns: ");
            //int cols = int.Parse(Console.ReadLine());
            //int[,] matrix = new int[rows, cols];
            //Console.WriteLine("Enter the cells of the matrix:");
            //for (int row = 0; row < rows; row++)
            //{
            //    for (int col = 0; col < cols; col++)
            //    {
            //        Console.Write($"matrix ( {rows}, {cols} ) ");
            //        matrix[row, col] = int.Parse(Console.ReadLine());
            //    }
            //}


            //for (int row = 0; row < matrix.GetLength(0); row++)
            //{
            //    for (int col = 0; col < matrix.GetLength(0); col++)
            //    {
            //        Console.Write(" " + matrix[row, col]);
            //    }
            //    Console.WriteLine();
            //}




            //Console.Write("Enter the number of the rows: ");
            //int rows = int.Parse(Console.ReadLine());

            //int[,] array = new int[rows, rows];

            //for (int i = 0; i < array.GetLength(0); i++)
            //{
            //    for (int j = 0; j < array.GetLength(1); j++)
            //    {
            //        Console.WriteLine($"Enter Your Element");
            //        array[i, j] = int.Parse(Console.ReadLine());



            //        Console.WriteLine($"matrix {i} , {j}  = " ,i , j);
            //    }

            //    Console.WriteLine();
            //}


            //int sumrightdiagonal = 0;
            //int sumliftdiagonal = 0;

            //for (int i = 0; i < rows; i++)
            //{
            //    sumrightdiagonal += array[i, rows - 1 - i];
            //    sumliftdiagonal += array[i, i];

            //}

            //int result = Math.Abs(sumrightdiagonal - sumliftdiagonal);

            //Console.WriteLine($"The absolute difference is {result}");

            
        }   

    }
}
